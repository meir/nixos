
(deflisten music_status
  :initial ""
  "zscroll --update-check true \"playerctl metadata --format '[{{ title }} - {{ artist }} {{duration(position)}}]'\""
)

(defwidget music []
  (box
    :class "audio"
    { "♫ ${music_status}" }
  )
)
