
(defwidget bspwm []
  (box
    (children)
  )
)

;(defpoll workspace_update
;  :interval "1s"
;  "./workspace \"${name}\""
;)

(defwidget workspace [name ?label]
  (button
    :onclick "bspc desktop -f \"${name}\""
    :class "workspace"

    {label ? label : name}
  )
)
